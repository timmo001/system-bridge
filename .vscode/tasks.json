{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Install application dependencies (Linux)",
      "type": "shell",
      "command": "pip install -r requirements_linux.txt"
    },
    {
      "label": "Install application dependencies (Windows)",
      "type": "shell",
      "command": "pip install -r requirements_windows.txt"
    }
  ]
}
