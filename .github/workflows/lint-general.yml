---
name: Lint general

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - dev
  pull_request:
    types:
      - opened
      - synchronize
  workflow_dispatch: {}

permissions: {}

jobs:
  lint:
    uses: timmo001/workflows/.github/workflows/lint-general.yml@68adb4ba625cbc742312fe526d57024f1ec4b493 # master
