---
name: Lint general

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - dev
  pull_request:
    types:
      - opened
      - synchronize
  workflow_dispatch: {}

permissions: {}

jobs:
  lint:
    uses: timmo001/workflows/.github/workflows/lint-general.yml@1ed6e00bfeecec20f59b49bb96a40969ccc8821b # master
