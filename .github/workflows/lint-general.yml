---
name: Lint general

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - dev
  pull_request:
    types:
      - opened
      - synchronize
  workflow_dispatch: {}

jobs:
  lint:
    uses: timmo001/workflows/.github/workflows/lint-general.yml@78079dac4b415ee56c5dc446bc532f4b41d5eebb # master
