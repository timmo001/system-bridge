---
name: Lint general

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - dev
  pull_request:
    types:
      - opened
      - synchronize
  workflow_dispatch: {}

permissions: {}

jobs:
  lint:
    uses: timmo001/workflows/.github/workflows/lint-general.yml@065e689c85cec1e7d025f6d074b2589e41d6bb2b # master
