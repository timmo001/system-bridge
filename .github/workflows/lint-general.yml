---
name: Lint general

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - dev
  pull_request:
    types:
      - opened
      - synchronize
  workflow_dispatch: {}

jobs:
  lint:
    uses: timmo001/workflows/.github/workflows/lint-general.yml@dd409a67cdb5d5b764f3fb5753df84fd4d0b2094 # master
