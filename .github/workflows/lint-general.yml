---
name: Lint general

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - dev
  pull_request:
    types:
      - opened
      - synchronize
  workflow_dispatch: {}

jobs:
  lint:
    uses: timmo001/workflows/.github/workflows/lint-general.yml@4a4f5b2e4e2bb512485627a3a0ae199fdcea2b6c # master
