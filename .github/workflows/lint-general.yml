---
name: Lint general

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - dev
  pull_request:
    types:
      - opened
      - synchronize
  workflow_dispatch: {}

permissions: {}

jobs:
  lint:
    uses: timmo001/workflows/.github/workflows/lint-general.yml@0ac664d85b0ad75f2d6bec537abece3db3c9f237 # master
