---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@0396f6681097e20d0d623589c60cec6fb187db50 # master
    permissions:
      pull-requests: write
      contents: write
