---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@ce3e421edb8e339f5405c70023aca7445270cd13 # master
    permissions:
      pull-requests: write
      contents: write
