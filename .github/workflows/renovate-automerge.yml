---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@ff67831f05ffa4f950a5505263207b8bb8f24369 # master
    permissions:
      pull-requests: write
      contents: write
