---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@29c1dbda36083c1d8d0b8ce74ede0b4acfeaf53a # master
    permissions:
      pull-requests: write
      contents: write
