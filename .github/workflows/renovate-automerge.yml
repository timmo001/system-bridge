---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@6d0087dcfd2e05578eff9ca03359dbcb876d7fba # master
    permissions:
      pull-requests: write
      contents: write
