---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@d220b93d3fb3dd498b56bd562bba64b0a00073b8 # master
    permissions:
      pull-requests: write
      contents: write
