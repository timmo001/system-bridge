---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@4a4f5b2e4e2bb512485627a3a0ae199fdcea2b6c # master
    permissions:
      pull-requests: write
      contents: write
