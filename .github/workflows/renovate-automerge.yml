---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@144b4a55b395ada426c5d5ba615874eb8b084fe1 # master
    permissions:
      pull-requests: write
      contents: write
