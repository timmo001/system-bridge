---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@2b050288cd02492c371a6ef5269817db163c002b # master
    permissions:
      pull-requests: write
      contents: write
