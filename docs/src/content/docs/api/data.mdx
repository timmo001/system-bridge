---
title: Data
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

_Get data from data modules._

## Authentication

This endpoint requires authentication via the `X-API-Token` header or the `token` header.

## Request

<Tabs syncKey="auth-header">
  <TabItem label="X-API-Token header">
    ```http
    GET /api/data/{module}
    X-API-Token: {token}
    ```
  </TabItem>
  <TabItem label="token header">
    ```http
    GET /api/data/{module}
    token: {token}
    ```
  </TabItem>
</Tabs>

`{module}` is one of: `battery`, `cpu`, `disks`, `displays`, `gpus`, `media`, `memory`, `networks`, `processes`, `sensors`, `system`.

## Response

The response format varies depending on the module requested. Here's an example:

```json title="Response"
{
    "boot_time": 1651433957.3015134,
    "fqdn": "Desktop",
    "hostname": "Desktop",
    "ip_address_4": "1.1.1.1",
    "mac_address": "aa:bb:cc:dd:ee:ff",
    "platform": "Windows",
    "platform_version": "10.0.22000",
    "uptime": 7.015625,
    "uuid": "abcd-efgh-abcd-efgh-abcd",
    "version": "5.0.2",
    "version_latest": "5.0.7",
    "version_newer_available": false
}
```
