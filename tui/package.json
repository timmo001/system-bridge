{
  "name": "system-bridge-tui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --compile --outfile ../dist/system-bridge-tui",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\""
  },
  "dependencies": {
    "@opentui/core": "^0.2.7",
    "effect": "^4.0.0-beta.66",
    "fuse.js": "^7.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "prettier": "^3.5.3"
  }
}
